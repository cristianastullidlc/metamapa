<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Hora Pico de cada Categoria</title>
    <link rel="stylesheet" href="/assets/dashboard.css">
</head>
<body class="dashboard-body">

<div class="dashboard-container">
    {{> templates/dashboard/_sidebar}}

    <main class="main-content">
        <header class="main-header">
            <h1>Hora Pico por Categoría</h1>
        </header>

        <section class="section">
            <div class="section-header">
                <h2>⏰ Estadísticas de Horas Pico</h2>
                {{#if reporte}}
                    <a class="btn btn-primary btn-icon" href="/descargar/estadisticas_categoria_horaspico.csv" download>
                        📥 Exportar
                    </a>
                {{/if}}
            </div>

            <div class="table-container">
                <table class="modern-table">
                    <thead>
                        <tr>
                            <th>Fecha</th>
                            <th>Categoría</th>
                            <th class="text-center">Horario Pico</th>
                            <th class="text-center">Cantidad de Hechos</th>
                        </tr>
                    </thead>
                    <tbody>
                    {{#if reporte}}
                        {{#each reporte}}
                            <tr>
                                <td>{{formatDateOnly fecha}}</td>
                                <td>{{this.categoria}}</td>
                                <td class="text-center"><span class="badge badge-primary">{{this.horapico}}</span></td>
                                <td class="text-center"><span class="badge badge-info">{{this.cantidad}}</span></td>
                            </tr>
                        {{/each}}
                    {{else}}
                        <tr>
                            <td colspan="4" class="text-center empty-cell">No hay datos disponibles</td>
                        </tr>
                    {{/if}}
                    </tbody>
                </table>
            </div>
        </section>

    </main>
</div>
</body>
</html>