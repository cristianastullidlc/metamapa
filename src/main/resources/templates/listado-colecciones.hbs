<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Seleccionar Colección</title>
    <link rel="stylesheet" href="/assets/dashboard.css">
</head>
<body class="dashboard-body">
<div class="dashboard-container">

    {{> templates/dashboard/_sidebar}}

    <main class="main-content">
        <header class="main-header">
            <h1>Colecciones</h1>
            <p class="subtitle">Gestiona las colecciones del sistema</p>
        </header>

        <section class="section">
            {{#if flash_message}}
                <div class="alert alert-success">{{flash_message}}</div>
            {{/if}}

            <div class="section-header">
                <h2>📚 Mis Colecciones</h2>
                <a href="/dashboard/colecciones/crear" class="btn btn-primary btn-sm">➕ Nueva Colección</a>
            </div>

            {{#if colecciones}}
                <div class="colecciones-grid">
                    {{#each colecciones}}
                        <div class="coleccion-card">
                            <div class="coleccion-info">
                                <h4>{{this.titulo}}</h4>
                                <p class="coleccion-desc">{{this.descripcion}}</p>
                            </div>
                            <div class="coleccion-acciones">
                                <a href="/dashboard/colecciones/modificar/{{this.id}}" class="btn btn-primary btn-sm">✏️ Editar</a>
                            </div>
                        </div>
                    {{/each}}
                </div>
            {{else}}
                <div class="empty-state">
                    <p>📚 No hay colecciones creadas</p>
                    <a href="/dashboard/colecciones/crear" class="btn btn-primary">➕ Crear Primera Colección</a>
                </div>
            {{/if}}
        </section>
    </main>
</div>
</body>
</html>